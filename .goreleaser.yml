version: 2

project_name: mnemoteca

before:
  hooks:
    - task download-darwin-libs

env:
  - CGO_ENABLED=1

builds:
  - id: "darwin"
    binary: mnemoteca
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=1
      - CGO_LDFLAGS=-Llib/darwin_{{ .Arch }} -Flib/darwin_{{ .Arch }} -L{{ .Env.SDK_PATH }}/usr/lib -F{{ .Env.SDK_PATH }}/System/Library/Frameworks
      - CGO_CFLAGS=-Iinclude -isysroot {{ .Env.SDK_PATH }}
    flags:
      - -tags=sqlite_fts5
    ldflags:
      - -w -X github.com/gandazgul/mnemoteca/cmd.Version={{.Version}} -X github.com/gandazgul/mnemoteca/cmd.Commit={{.Commit}} -X github.com/gandazgul/mnemoteca/cmd.Date={{.Date}} -X github.com/gandazgul/mnemoteca/cmd.Release=true

archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
  groups:
    - title: Features
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 0
    - title: Bug Fixes
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 1
    - title: Others
      order: 999
